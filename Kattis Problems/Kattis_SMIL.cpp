#include <iostream>
#include <string>
#include <cmath>
#include <iomanip>
#include <fstream>
using namespace std; 

int main() {
    string smileForest;
    int forestLen;
    cin >> smileForest;

    forestLen = smileForest.length();

    for (int i = 0; i < forestLen; i++){
        if (smileForest[i] ==  
    }

    
return 0;
}