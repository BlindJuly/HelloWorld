#include <iostream>
#include <string>
#include <cmath>
#include <iomanip>
using namespace std; 

/*
1. 2 week vacation per year (10 days)
2.  if employed 3 years, then, 3 weeks vacation (15 days)
3. if employed > 3 years, then, 2 days per year, capped at 6 years. (2+2+2 = 6)
4. if employed > 6 years, then, 1 day per year (no cap)
5. Takes in years of service and outputs vacation days
*/

int main() {
    int years_emp;
    int vac_total;
    const int Vac_base = 10;

    cout << "Please enter your total amount of years employed to our company: " << endl;
    cin >> years_emp;




return 0;
}